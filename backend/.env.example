# Server
PORT=3000

# MySQL (database name: student)
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=student

# Optional: restrict CORS (default allows all)
# CORS_ORIGIN=http://localhost:5173

# JWT (use a strong secret in production)
JWT_SECRET=your-secret-key-change-in-production
# JWT_EXPIRES_IN=7d

# Default admin (created on first run if no admin exists)
ADMIN_EMAIL=admin@pareeksha.local
ADMIN_PASSWORD=admin123
